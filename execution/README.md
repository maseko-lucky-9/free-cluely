# Execution Layer

This directory contains deterministic Python scripts for the Beast Agent.

## Guidelines

- Scripts should be focused, reliable, and testable.
- Use `../.env` for secrets.
- Handle errors gracefully.
- Minimize dependencies.

## Usage

The Agent calls these scripts based on the Directives in `../directives/`.
